package com.example.mockserver.rest.api

import com.example.mockserver.rest.api.data.DataPresenter

/**
 * Data Presenter Api is an abstract layer that informs presenter what to pass further to the view.
 */
class DataPresenterApi(presenter: DataPresenter<*>) {
}